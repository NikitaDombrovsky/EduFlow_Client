# Экран редактора учебных материалов

## Описание

Полнофункциональный экран для редактирования учебных материалов с удобным интерфейсом, поддержкой drag-and-drop переупорядочивания и встроенной валидацией.

## Основные возможности

### ✏️ Редактирование фрагментов
- Inline редактирование заголовков и содержимого
- Поддержка многострочного текста
- Автоматическое сохранение изменений
- Подсказки для каждого типа фрагмента

### 🎯 Типы фрагментов
- **Теория** - теоретический материал, определения
- **Пример кода** - код с пояснениями (поддержка языков программирования)
- **Практическое задание** - задачи для студентов
- **Визуализация** - описание схем и диаграмм

### 🔄 Управление структурой
- Drag-and-drop переупорядочивание фрагментов
- Добавление новых фрагментов любого типа
- Удаление фрагментов с подтверждением
- Автоматическая нумерация порядка

### 💾 Работа с изменениями
- Индикатор несохраненных изменений
- Отмена всех изменений одной кнопкой
- Предупреждение при выходе без сохранения
- Batch-сохранение всех фрагментов

### 📤 Экспорт
- Экспорт в PDF
- Экспорт в PowerPoint презентацию
- Сохранение текущего состояния перед экспортом

## Структура компонентов

```
material_builder/presentation/
├── screens/
│   └── material_editor_screen.dart          # Главный экран редактора
└── components/
    ├── fragment_editor_card.dart            # Карточка редактирования фрагмента
    ├── fragment_type_selector.dart          # Выбор типа нового фрагмента
    └── fragment_editor_section.dart (обновлен) # Добавлена кнопка редактирования
```

## Использование

### Переход к редактору

Из экрана просмотра обработанных материалов:

```dart
// В _HeaderSection добавлена кнопка "Редактировать"
ElevatedButton.icon(
  onPressed: () => _openEditor(context),
  icon: const Icon(Icons.edit),
  label: const Text('Редактировать'),
)
```

### Навигация

```dart
Navigator.of(context).push(
  MaterialPageRoute(
    builder: (_) => BlocProvider.value(
      value: context.read<MaterialBuilderBloc>(),
      child: MaterialEditorScreen(material: material),
    ),
  ),
);
```

## Основные компоненты

### MaterialEditorScreen

Главный экран редактора с поддержкой:
- Отслеживания изменений (`_hasChanges`)
- Сохранения/отмены изменений
- ReorderableListView для drag-and-drop
- FloatingActionButton для добавления фрагментов

**Ключевые методы:**
- `_updateFragment(index, updated)` - обновление фрагмента
- `_deleteFragment(index)` - удаление фрагмента
- `_onReorder(oldIndex, newIndex)` - переупорядочивание
- `_saveChanges()` - сохранение всех изменений
- `_resetChanges()` - отмена всех изменений

### FragmentEditorCard

Расширяемая карточка редактирования с:
- Компактным заголовком (collapsed state)
- Полной формой редактирования (expanded state)
- Выбором типа через ChoiceChip
- Валидацией и подсказками

**Особенности:**
- Автоматическое уведомление родителя об изменениях
- Drag handle для переупорядочивания
- Счетчик символов
- Подсказки в зависимости от типа фрагмента

### FragmentTypeSelector

Диалог выбора типа фрагмента с:
- Визуальными карточками для каждого типа
- Иконками и описаниями
- Немедленным созданием фрагмента после выбора

## UX особенности

### 🎨 Визуальные индикаторы
- **Иконка drag_handle** - показывает возможность перетаскивания
- **Цветные иконки типов** - быстрая идентификация типа фрагмента
- **Оранжевый chip** - индикатор несохраненных изменений
- **Счетчик символов** - контроль объема контента

### ⚠️ Предупреждения
- Подтверждение удаления фрагмента
- Предупреждение при выходе с несохраненными изменениями
- PopScope для перехвата системной кнопки "Назад"

### 📊 Статус-бар
- Общее количество фрагментов
- Индикатор изменений
- Быстрый доступ к функциям сохранения/отмены

### 🎯 Пустое состояние
- Центрированное сообщение
- Призыв к действию
- Прямая кнопка добавления первого фрагмента

## Примеры состояний

### Список с фрагментами
```dart
// Показывается ReorderableListView
ReorderableListView.builder(
  onReorder: _onReorder,
  itemCount: _fragments.length,
  itemBuilder: (context, index) {
    return ReorderableDragStartListener(
      key: ValueKey(fragment.id),
      index: index,
      child: FragmentEditorCard(...),
    );
  },
)
```

### Пустой список
```dart
// Показывается приглашение добавить первый фрагмент
Center(
  child: Column(
    children: [
      Icon(Icons.description_outlined),
      Text('Нет фрагментов'),
      ElevatedButton.icon(
        onPressed: _showAddFragmentDialog,
        label: Text('Добавить фрагмент'),
      ),
    ],
  ),
)
```

## Интеграция с BLoC

### Сохранение изменений
```dart
void _saveChanges() {
  // Пакетное сохранение всех фрагментов
  for (final fragment in _fragments) {
    context.read<MaterialBuilderBloc>().add(
      MaterialBuilderUpdateFragmentEvent(fragment: fragment),
    );
  }
  setState(() => _hasChanges = false);
}
```

### Экспорт материала
```dart
void _exportMaterial(String format) {
  final updatedMaterial = ProcessedMaterialEntity(
    documentId: widget.material.documentId,
    originalFileName: widget.material.originalFileName,
    fragments: _fragments,
    processedAt: widget.material.processedAt,
  );
  
  context.read<MaterialBuilderBloc>().add(
    MaterialBuilderGenerateOutputEvent(
      material: updatedMaterial,
      outputFormat: format,
    ),
  );
}
```

## Горячие клавиши (планируется)

- `Ctrl/Cmd + S` - сохранить изменения
- `Ctrl/Cmd + Z` - отменить изменения
- `Ctrl/Cmd + N` - добавить новый фрагмент
- `Delete` - удалить выбранный фрагмент

## TODO: Будущие улучшения

### Редактор кода
- [ ] Подсветка синтаксиса для примеров кода
- [ ] Автодополнение кода
- [ ] Форматирование кода

### Markdown поддержка
- [ ] Live preview для Markdown
- [ ] Вставка изображений
- [ ] Таблицы и списки

### Совместная работа
- [ ] Конфликты при одновременном редактировании
- [ ] История изменений
- [ ] Комментарии к фрагментам

### Шаблоны
- [ ] Готовые шаблоны фрагментов
- [ ] Импорт фрагментов из других материалов
- [ ] Библиотека часто используемых фрагментов

## Технические детали

### State Management
- Локальный state для списка фрагментов (`_fragments`)
- Флаг изменений (`_hasChanges`)
- TextEditingController для каждого поля ввода

### Performance
- ReorderableListView с ключами по `fragment.id`
- Мемоизация через `ValueKey`
- Ленивая загрузка больших текстов (планируется)

### Accessibility
- Semantic labels для иконок
- Подсказки (tooltips) для кнопок
- Поддержка навигации клавиатурой

## Соответствие Clean Architecture

✅ **Domain** - использует `MaterialTemplateEntity`, `ProcessedMaterialEntity`
✅ **Presentation** - только UI логика, работа через BLoC
✅ **Separation of Concerns** - каждый компонент отвечает за свою функцию
✅ **Reusability** - компоненты переиспользуемы

## Следование RFC проекта

✅ **Документация** - полная документация всех классов и методов
✅ **Именование** - Screen, Card, Selector суффиксы
✅ **Структура** - presentation/screens и presentation/components
✅ **State Management** - через BLoC паттерн
✅ **UI Kit** - использование app_snackbar, app_box
